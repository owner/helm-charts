---
# yaml-language-server: $schema=https://raw.githubusercontent.com/helm-unittest/helm-unittest/main/schema/helm-testsuite.json
suite: certificateClusterIssuer - presence
templates:
  - common.yaml
values:
  - ../_values/controllers_main_default_container.yaml
tests:
  - it: a ClusterIssuer is not created by default
    asserts:
      - not: true
        containsDocument:
          kind: ClusterIssuer
          apiVersion: cert-manager.io/v1
          any: true

  - it: a ClusterIssuer is not created when empty
    set:
      certificateClusterIssuers: {}
    asserts:
      - not: true
        containsDocument:
          kind: ClusterIssuer
          apiVersion: cert-manager.io/v1
          any: true

  - it: a ClusterIssuer is created when configured
    set:
      certificateClusterIssuers:
        main:
          spec:
            acme:
              server: https://acme-v02.api.letsencrypt.org/directory
              email: admin@example.com
              privateKeySecretRef:
                name: letsencrypt-account-key
    documentSelector:
      path: $[?(@.kind == "ClusterIssuer")].metadata.name
      value: release-name
    asserts:
      - equal:
          path: spec.acme.server
          value: https://acme-v02.api.letsencrypt.org/directory
      - equal:
          path: spec.acme.email
          value: admin@example.com
      - equal:
          path: spec.acme.privateKeySecretRef.name
          value: letsencrypt-account-key

  - it: a ClusterIssuer is not created when disabled
    set:
      certificateClusterIssuers:
        main:
          enabled: false
          spec:
            acme:
              server: https://acme-v02.api.letsencrypt.org/directory
              email: admin@example.com
              privateKeySecretRef:
                name: letsencrypt-account-key
    asserts:
      - not: true
        containsDocument:
          kind: ClusterIssuer
          apiVersion: cert-manager.io/v1
          any: true

  - it: spec can render Helm template
    set:
      certificateClusterIssuers:
        main:
          spec:
            acme:
              server: https://acme-v02.api.letsencrypt.org/directory
              email: "{{ .Release.Name }}@example.com"
              privateKeySecretRef:
                name: letsencrypt-account-key
    documentSelector:
      path: $[?(@.kind == "ClusterIssuer")].metadata.name
      value: release-name
    asserts:
      - equal:
          path: spec.acme.email
          value: RELEASE-NAME@example.com
